interface footerProps {
    brand: boolean
}
export default function Footer(props : footerProps) {
    if(props.brand) {
        return (
            <div></div>
        )
    }else {
        return (
            <div></div>
        )
    }
}